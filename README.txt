GIT LEAKS

if running docker:
    > docker build -t <image_name> .
    > docker run -it <image_name>

if running locally:
    > pip install -r requirements.txt
    > python3 main.py

The script will ask you the output format and save the file on a new folder output/.